# Preload Tiles

See https://openlayers.org/en/latest/examples/preload.html

import React from "react";
import "ol/ol.css";
import { Map } from "@react-ol/fiber";

export const ExamplePreload = () => (
  <Map className="rounded-xl overflow-hidden mt-4">
    <olView initialCenter={[0, 0]} initialZoom={2} />
    <olLayerTile preload={Infinity}>
      <olSourceOSM />
    </olLayerTile>
  </Map>
);

<ExamplePreload />

```tsx
import React from "react";
import "ol/ol.css";
import { Map } from "@react-ol/fiber";

export const ExamplePreload = () => (
  <Map>
    <olView initialCenter={[0, 0]} initialZoom={2} />
    <olLayerTile preload={Infinity}>
      <olSourceOSM />
    </olLayerTile>
  </Map>
);
```
